import ContentLayout from './content';
import HeaderMenuLayout from './header-menu';

const MainLayouts = () => {
  return (
    <div>
      <HeaderMenuLayout />
      <ContentLayout />
    </div>
  );
};

export default MainLayouts;
